name: adas-swc-workflow

on:
  workflow_call:
    inputs:
      swc-name:
        required: true
        type: string

jobs:
  experiment:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo ${{ inputs.swc-name }}
